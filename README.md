# Task Manager — управління проєктами та завданнями

Це односторінковий додаток (SPA) для управління проєктами та завданнями всередині них.

## 🌐 Демо

**Онлайн демо:** [https://task-manager-fa2.vercel.app/](https://task-manager-fa2.vercel.app/)

> ⚠️ **Увага:** Демо-версія використовує мокове API, тому функціонал створення, редагування та видалення проєктів/завдань працює тільки локально. Онлайн версія призначена для ознайомлення з інтерфейсом.

## ✨ Основний функціонал

- ✅ Список усіх проєктів з пошуком
- ✅ Додавання, редагування та видалення проєктів через модальне вікно
- ✅ Детальна сторінка проєкту з двома режимами перегляду:
  - **Таблиця завдань** — сортування, фільтрація за виконавцем та статусом
  - **Канбан-дошка** — drag-and-drop між статусами (todo / in-progress / done)
- ✅ Створення / редагування / видалення завдань з валідацією
- ✅ Збереження стану додатку (проєкти, завдання) у `json-server` (локально)
- ✅ Перемикання режиму перегляду (таблиця / канбан) зберігається в `localStorage` для кожного проєкту окремо

## 🛠 Стек технологій

- **Vue** — Composition API + `<script setup>`
- **TypeScript** — повна типізація проєктів, завдань, сторів
- **Pinia** — менеджер стану + плагін `pinia-plugin-persistedstate`
- **Vue Router** — навігація між сторінками
- **Axios** — HTTP-запити до API
- **JSON Server** — мок-бекенд для локальної розробки
- **SCSS** — стилізація (змінні, глобальні стилі)
- **Drag & Drop API** — нативний HTML5 Drag & Drop
- **Vite** — швидкий інструмент збірки

## 📁 Структура проєкту

```
task-manager/
├── src/
│   ├── api/
│   │   └── index.ts              # API клієнт (axios)
│   ├── assets/
│   │   └── main.scss             # Глобальні стилі
│   ├── components/
│   │   ├── modals/
│   │   │   └── TaskModal.vue     # Модалка створення/редагування завдання
│   │   ├── ProjectTable.vue      # Таблиця проєктів
│   │   ├── TaskTable.vue         # Таблиця завдань
│   │   └── KanbanBoard.vue       # Канбан-дошка з drag-and-drop
│   ├── router/
│   │   └── index.ts              # Маршрутизація
│   ├── stores/
│   │   ├── projects.ts           # Pinia store для проєктів
│   │   └── tasks.ts              # Pinia store для завдань
│   ├── types/
│   │   ├── project.ts            # Типи для проєктів
│   │   └── task.ts               # Типи для завдань
│   ├── views/
│   │   ├── HomeView.vue          # Головна сторінка зі списком проєктів
│   │   └── ProjectView.vue       # Детальна сторінка проєкту
│   ├── App.vue
│   └── main.ts
├── db.json                        # База даних для json-server
├── package.json
└── vite.config.ts
```

## 🚀 Як запустити локально

### 1. Клонуйте репозиторій

git clone https://github.com/your-username/task-manager.git
cd task-manager

````

### 2. Встановіть залежності

```bash
npm install
````

### 3. Запустіть проєкт

**Потрібно відкрити 2 термінали:**

**Термінал 1** — Frontend (Vite):

```bash
npm run dev
```

**Термінал 2** — Backend (JSON Server):

```bash
npm run dev:server
```

Додаток буде доступний за адресою: **http://localhost:5173**  
API працює на: **http://localhost:3001**

> 💡 **Альтернатива:** Якщо у вас встановлений `concurrently`, можна запустити обидва сервери одночасно:
>
> ```bash
> npm run dev:all
> ```

### 4. Збірка для продакшну

```bash
npm run build
```

Готові файли будуть у папці `dist/`.

## 📝 Доступні скрипти

```json
{
  "dev": "vite", // Запуск frontend
  "dev:server": "json-server --watch db.json --port 3001", // Запуск backend
  "dev:all": "concurrently \"npm run dev\" \"npm run dev:server\"", // Запуск обох
  "build": "vue-tsc -b && vite build", // Збірка для продакшну
  "preview": "vite preview" // Попередній перегляд збірки
}
```

## 🎯 Особливості реалізації

### Drag & Drop у Канбан-дошці

- Використовується нативний HTML5 Drag & Drop API
- Підтримка переміщення завдань між колонками (зміна статусу)
- Зміна порядку всередині колонок
- Плавні анімації та візуальний фідбек

### Менеджер стану (Pinia)

- Окремі stores для проєктів (`projects.ts`) та завдань (`tasks.ts`)
- Асинхронні дії для роботи з API
- Обробка помилок та станів завантаження

### Збереження стану

- Режим перегляду (таблиця/канбан) зберігається в `localStorage` для кожного проєкту
- Проєкти та завдання зберігаються в `db.json` через `json-server`

### Типізація

- Повна типізація TypeScript для всіх компонентів, stores та API
- Інтерфейси `Project` та `Task` у папці `types/`

## 🐛 Відомі обмеження

- Демо-версія на Vercel не має повноцінного бекенду, тому всі зміни втрачаються після перезавантаження
- Для повного функціоналу потрібно запускати проєкт локально з `json-server`

## 📄 Ліцензія

MIT

## 👤 Автор

Ваше ім'я — [GitHub](https://github.com/your-username)

---

**Зроблено з ❤️ за допомогою Vue 3 + TypeScript**
